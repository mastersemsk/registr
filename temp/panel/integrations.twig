<!doctype html>
<html lang="ru">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <title>Настройка Яндекс/Google Disk</title>

    <link rel="canonical" href="http://e-pos/panel/integrations">

<link href="/css/bootstrap.min.css" rel="stylesheet"> 
    <!-- Custom styles for this template -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.3/font/bootstrap-icons.css" rel="stylesheet">
    <!-- Custom styles for this template -->
    <link href="/css/dashboard.css" rel="stylesheet">
  </head>
  <body>  

<header class="navbar sticky-top bg-dark flex-md-nowrap p-0 shadow" data-bs-theme="dark">
  <a class="navbar-brand col-md-3 col-lg-2 me-0 px-3 fs-6 text-white" href="/">e-POS</a>
</header>

<div class="container-fluid">
  <div class="row">
        {% include '/panel/_menu.twig' %}
    <main class="col-md-9 ms-sm-auto col-lg-10 px-md-4">
      <div class="d-flex justify-content-between flex-wrap flex-md-nowrap align-items-center pt-3 pb-2 mb-3 border-bottom">
        <h1 class="h2">Яндекс|Google Disk</h1>
      </div>
      <h2>Загрузка файлов Яндекс|Google Disk</h2>
      {% if result.error %}
          <p>{{ result.error }}</p>
      {% endif %}
      {% if result.token %}
      <p>Загрузка файла на Яндекс Диск</p>
      <form enctype="multipart/form-data" action="/panel/userfail" method="post">
      <div class="mb-3">
        <input type="hidden" name="MAX_FILE_SIZE" value="1000000">
        <label for="yandexFile" class="form-label">Выбрать файл для загрузки</label>
         <input class="form-control" type="file" id="yandexFile" name="diskfile">
         <input type="hidden" name="yandexfile" value="1">
      </div>
      <button type="submit" class="btn btn-primary">Загрузить</button>
      </form>
       <p>Загрузка файла на Google Disk</p>
      <form enctype="multipart/form-data" action="/panel/userfail" method="post">
      <div class="mb-3">
        <label for="googleFile" class="form-label">Выбрать файл для загрузки</label>
        <input type="hidden" name="MAX_FILE_SIZE" value="1000000">
        <input class="form-control" type="file" id="googleFile" name="diskfile">
        <input type="hidden" name="googlefile" value="1">
      </div>
      <button type="submit" class="btn btn-primary">Загрузить</button>
      </form>     
      {% else %}
      <p>Чтобы подключить ваш диск нужно отправить запрос</>
      <form action="" method="post">
      <div class="mb-3">
      <div class="form-check">
          <input class="form-check-input" type="radio" name="disk" value="yandex" id="yandex">
          <label class="form-check-label" for="yandex">
             Яндекс Диск
          </label>
      </div>
      <div class="form-check">
          <input class="form-check-input" type="radio" name="disk" value="google" id="google" checked>
         <label class="form-check-label" for="google">
               Google Disk
         </label>
      </div>
      </div>
       <button type="submit" class="btn btn-primary">Отправить</button>
      </form>
      {% endif %}
    </main>
  </div>
</div>
<script src="/js/bootstrap.bundle.min.js"></script>

    <script src="https://cdn.jsdelivr.net/npm/chart.js@4.2.1/dist/chart.umd.min.js" integrity="sha384-gdQErvCNWvHQZj6XZM0dNsAoY4v+j5P1XDpNkcM3HJG1Yx04ecqIHk7+4VBOCHOG" crossorigin="anonymous"></script>
	<script src="/js/dashboard.js"></script>
	</body>
    </html>